let RTMClient,WebClient;_636‍.w("@slack/rtm-api",[["RTMClient",["RTMClient"],function(v){RTMClient=v}]]);_636‍.w("@slack/web-api",[["WebClient",["WebClient"],function(v){WebClient=v}]]);const dotenv = require('dotenv');
dotenv.config();
const packageJson = require('./package.json')



const {getStep, responseMessages} = require('./src/flow')


//environment variables
const SLACK_OAUTH_TOKEN = process.env.SLACK_OAUTH_TOKEN;
const DEFAULT_MESSAGE = process.env.DEFAULT_MESSAGE;


const rtm = new RTMClient(SLACK_OAUTH_TOKEN)
const web = new WebClient(SLACK_OAUTH_TOKEN)

// start the bot
rtm.start()
  .catch(_636‍.g.console.error)

rtm.on('ready', async ()=>{
  console.log('bot started')
})
